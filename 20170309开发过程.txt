编辑会员页面能根据会员所在城市ID显示多级选择框，之后再增加联动功能

    编辑会员页面能根据会员所在城市ID显示多级选择框详细思路过程：
    //要显示所在地区的那一级的选择框
    //（如果他是天河区的，那这个选择框还要显示跟天河区同级同上级地区选项，那么要完成这一点就需要获取天河区的上级地区）
    //可以使用 根据城市ID返回它的所有上级城市ID数组 传 天河区 ID过去，就会获取到 广州市、广东省 

    //还显示所在地区的所有上级的选择框（这些选择框也是要显示跟这一级地区同级的其它兄弟地区）
    //因为上级地区可能会有多个，所以要用循环。上面用天河区ID获取到的所有上级只有 广州市、广东省 如果就这样循环，那只会显示 两个选择框
    //这样还不够，还要加上显示天河区的这个三级选择框，所以这样的话可以把天河区这个ID也加入到 所有上级城市ID数组 里
    //那这样这个数组就会有 广东省 、广州市、天河区
    //如果现在循环这个数组，在循环体内显示下拉选择框 就能够显示三个下拉选择框
    //
    //接下来面临的问题就是怎么这三个下拉选择框分别显示不同级别的选择
    //这个问题看似复杂其实也不难，因为在数组里有了三个城市ID，最高级别的城区，是数组下标为0的元素，第二级的是下标为1的元素，第三级是下标为2的元素……
    //所以在显示这数组是下标为0的城市选择框时，那选项就是一级城市，获取某个ID的城市的下级城市也是有写好的函数的 getCityData()
    //循环到下标为1时， 要获取二级城市选项，也就是获取这二级城市的父级城市的所有下级城市 其实就用 数组[$key-1] 这样就能获取到它的上级城市ID
    //获取到它的上级城市ID 之后再想要获取它的所有下级城市 getCityData(它的上级城市ID)
    //
    //还要让所有每一级的城市被选中
    //$arr['value'] = $value;//预选值  value=这个值的选项就会被选中 循环到的地区ID

    增加联动功能 把城市联动功能的js代码放到city.js里


城市管理

城市列表实现点击城市名进入显示该城市的下级城市
1、给城市列表里的城市名称加上链接，链接还是链接到当前城市列表页面，只是多加id=当前城市的id的参数
2、给城市控制器的城市列表操作，增加获取传来的id，然后根据ID设置不同的查询条件

没有符合查询条件的列表页面增加显示 找不到记录

进入广州市的下级城市列表页面 列表的上面要这样显示： 地区列表/广东省/广州市 共13条记录
进入天河区的下级城市列表页面 列表的上面要这样显示： 地区列表/广东省/广州市/天河区 共3条记录

==控制器实现过程==
1.先声明两个空数组：所有上级城市ID数组，所有上级城市名称数组
1.先获取传来的城市ID的所有上级城市ID数组
2.再把传来的城市ID也加入到所有上级城市ID数组里
3.循环所有上级城市ID数组{
    4. 查询循环到的城市ID的城市的名称 赋值给 所有上级城市名称数组 的新增元素
}
4. 把 所有上级城市ID数组，所有上级城市名称数组 传给模板文件

==模板文件实现过程==
5.循环 所有上级城市名称数组{
    6. echo 循环到的元素和加上链接
}



公共函数
    增加一个根据城市ID返回它的所有上级城市ID数组的函数
    增加一个根据城市id返回城市名称的函数
    增加一个根据职业ID返回职业名称的函数 jobIdToName（会员列表可以很简单就实现显示职业了）


function getParentCityId($id){//根据城市ID返回它的所有上级城市ID数组的函数
    $idArr[0] = substr($id,0,2);//取出1级地区编号部分
    $idArr[1] = substr($id,2,2);//取出2级地区编号部分
    $idArr[2] = substr($id,4,2);//取出3级地区编号部分
    $idArr[3] = substr($id,6,3);//取出4级地区编号部分

    $temp = $idArr[0] . '00' . '00' . '000';//取得这个传来的城市ID的1级城市ID
    if($id != $temp){
        $reArr[] = $temp;//给$reArr这个数组增加第1个元素，值是 传来的城市ID的1级城市ID
        $temp = $idArr[0] . $idArr[1] . '00' . '000';//取得这个传来的城市ID的2级城市ID
        if($id != $temp){
            $reArr[] = $temp;//给$reArr这个数组增加第2个元素，值是 传来的城市ID的2级城市ID
            $temp = $idArr[0] . $idArr[1] . $idArr[2] . '000';//取得这个传来的城市ID的3级城市ID
        }
        if($id != $temp)$reArr[] = $temp;//给$reArr这个数组增加第3个元素，值是 传来的城市ID的3级城市ID
        return $reArr;
    }
}


function 根据城市id返回城市名称的函数(城市id){
    1、$变量 = 获取这个 城市id 的 所有上级城市ID数组
    2、$父城市ID变量 = 从它的所有上级城市ID数组当中取出它的父城市ID (注意 判断所有上级城市ID数组 是不是为空，为空的话，父城市ID就用0)
    3、$所有下级城市 = 根据这个父城市ID获到它的所有下级城市
    4、循环它的所有下级城市{
        4.1、在循环体内判断 循环到的城市id是否跟传来的城市id相等，如果相等就返回 这个循环到的城市名称
        if(循环到的城市id === 传来的城市id)
    }
}

编辑会员提交
要增加判断用户名不能跟别人的重复

修改管理员时增加验证用户名不要跟别人重复

左侧菜单系统设置 里面的 添加修改页参考 是没用的，去掉它
左侧菜单系统设置 里面的 管理员设置 改为管理员列表
管理员列表页面的修改密码链接去掉，因为在编辑员工页面就可以修改密码
改好左侧菜单的修改密码的链接，和做好相应的控制器实现修改自己的用户名和密码

会员列表页面增加显示上次登录地点，和调整状态列对应表格的th


实现修改会员资料的功能
修改会员页面里的地址输入框宽度要改大

城市管理
 城市列表页面上面的添加城市链接去掉，改为放到列表里面（链接上循环到的城市ID）
 这个点某个城市后面的添加城市链接之后，进添加城市页面，就能够显示该城市和它的所有上级城市的下拉选择框，还会预选中，该城市和它的上级城市
 这样添加城市的模板页面，会需要获取当前城市的所有上级城市和他们对应的下拉选择框数据……参考会员编辑页面里的地区选择