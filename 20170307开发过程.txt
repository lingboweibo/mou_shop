会员表的stats这个状态字段去掉，因为现在是用了类似员工管理里面的代码，是用is_del表示的状态的，1删除 0正常 2停用

会员管理

地区和职业 先做好数据库，暂不做后台管理
地区表用 MyISAM
职业表也用 MyISAM
这是MyISAM的特点是查询快，修改慢，不支持事务，不支持外键

是因为这些地区表和职业表很少会修改，但是经常需要读取，暂时也不打算用它的外键功能

1.首先是在控制器的操作里读取职业表的数据，
2.再把读取到的数据传给模板文件，
3.然后在模板文件就可以接收传来的数据来使用，用来在页面上显示

练习，读取城市表一级城市(一般是省级)传给修改会员的模板文件，让模板文件 所在地区后面下拉选择框，能够下拉选择一级城市。
1.首先是在控制器的操作里读取城市表的数据（条件是一级城市），
2.再把读取到的数据传给模板文件，
3.然后在模板文件就可以接收传来的数据来使用，用来在页面上显示

选择字段

职业表的job_name字段，我觉得这个字段名太长，改为用name作为字段名了

排序

读取职业表数据 读取城市数据，在多个页面都需要使用，后台需要使用，前台也需要使用，所以觉得应该做成一个公共函数，放到公共模块的公共函数里面去

练习把 读取职业表数据和读取城市数据的功能做到公共模块的公共函数里面去，然后在添加会员和编辑会员的页面获取这两个数据，显示下拉选择框

因为职业表的数据的读取可能会比较频繁，但职业表里的数据不会经常更新，可以把职业数据放到缓存里，以后要读取时可以直接从缓存读取可加快速度
城市表的数据也是可以放到缓存里

更新时要删除缓存,以后做一个删除职业数据缓存的功能，添加城市或删除城市或修改城市时也要把对应的城市缓存删除掉

在公共函数文件增加一个生成select下拉菜单的HTML的函数，换行符用PHP_EOL